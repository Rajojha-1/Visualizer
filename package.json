{
  "name": "ai-pseudo-compiler-visualizer",
  "private": true,
  "workspaces": ["frontend", "backend", "shared"],
  "scripts": {
    "postinstall": "npm --prefix shared run build",
    "dev": "npm --prefix shared run build && concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix shared run build && npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix shared run build && npm --prefix backend run build && npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
